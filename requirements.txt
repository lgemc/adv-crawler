# Core crawling library
crawlee[playwright]>=0.4.0

# HTML to Markdown conversion
html2text>=2020.1.16

# Configuration
PyYAML>=6.0

# Async support (included with crawlee but explicit for clarity)
asyncio>=3.4.3

# URL parsing (standard library, but listing for completeness)
# urllib3 is included with requests which crawlee uses

# Optional: For better performance
aiofiles>=23.0.0

# Optional: For colored terminal output
colorama>=0.4.6